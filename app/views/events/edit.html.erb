<!--
利用rails 提供的helper form_for產生表單
form_for後第一個空格填入一個model產生的物件，這邊是由controller傳遞過來的@event
url後帶入一個hash，rails 會依據給的controller與action產生對應的網站，修改資料的action我們預計使用update
另外需要補上想要對哪一個id進行修改
form_for會透過這些參數替我們產生適當的 form tag
-->
<%= form_for @event, :url => { :controller => "events" , :action => "update" , :id => @event} do |f| %>
    <%= f.label :name , "Name" %>
    <!--為 name 建立一個text_field的輸入框 跟new不同的是 rails很聰明的會把資料庫的內容先帶入-->
	<%= f.text_field :name %>

    <%= f.label :description , "Description" %>
    <!--為 description 建立一個text_area的輸入框 跟new不同的是 rails很聰明的會把資料庫的內容先帶入--><%= f.text_field :description %>

    <!--使用submit方法建立一個送出的按鈕-->
    <%= f.submit "update" %>

<% end %>